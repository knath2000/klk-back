# Frontend URL for CORS (CRITICAL for Vercel deployment)
# Format: comma-separated list of allowed origins
# IMPORTANT: No trailing slashes, include protocol (https://)
FRONTEND_URL=https://klk-front.vercel.app,http://localhost:3000

# For multiple Vercel environments, add them like this:
# FRONTEND_URL=https://klk-front.vercel.app,https://klk-front-git-feature-branch.vercel.app,http://localhost:3000

# Server Configuration
PORT=3001
NODE_ENV=production
HOST=0.0.0.0

# LLM Configuration (for LangDB)
LANGDB_GATEWAY_URL=https://api.us-east-1.langdb.ai/your-org-id/v1
LANGDB_API_KEY=your_actual_api_key
LANGDB_MODEL=anthropic/claude-sonnet-4
ENABLE_STREAMING=true
REQUEST_TIMEOUT=30000

# Railway Deployment Notes:
# 1. Set FRONTEND_URL to your actual Vercel domain
# 2. Ensure no trailing slashes in URLs
# 3. Include https:// protocol for production domains
# 4. Add localhost for local development testing
